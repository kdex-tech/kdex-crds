---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: kdexclusterpagearchetypes.kdex.dev
spec:
  group: kdex.dev
  names:
    kind: KDexClusterPageArchetype
    listKind: KDexClusterPageArchetypeList
    plural: kdexclusterpagearchetypes
    shortNames:
    - kdex-c-pa
    singular: kdexclusterpagearchetype
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KDexClusterPageArchetype is the Schema for the kdexclusterpagearchetypes
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of KDexClusterPageArchetype
            properties:
              content:
                description: content is a go string template that defines the structure
                  of an HTML page.
                example: <!DOCTYPE html>\n<html lang="{{ .Language }}">\n  <head>\n    {{
                  .Meta }}\n    {{ .Title }}\n    {{ .Theme }}\n    {{ .HeadScript
                  }}\n  </head>\n  <body>\n    <header>\n      {{ .Header }}\n    </header>\n    <nav>\n      {{
                  .Navigation["main"] }}\n    </nav>\n    <main>\n      {{ .Content["main"]
                  }}\n    </main>\n    <footer>\n      {{ .Footer }}\n    </footer>\n    {{
                  .FootScript }}\n  </body>\n</html>
                minLength: 5
                type: string
              defaultFooterRef:
                description: defaultFooterRef is an optional reference to a KDexPageFooter
                  resource. If not specified, no footer will be displayed. Use the
                  `.Footer` property to position its content in the template.
                properties:
                  clustered:
                    description: |-
                      Clustered, if set to true, ensures that the cluster scoped Kind of the referent will be lookup up. If the
                      referring resource is cluster scoped, then Clustered is implicitly true.
                      Defaulted to nil.
                    type: boolean
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: namespace must not be set when clustered is true
                  rule: '!(self.clustered == true) || self.namespace == ”'
              defaultHeaderRef:
                description: defaultHeaderRef is an optional reference to a KDexPageHeader
                  resource. If not specified, no header will be displayed. Use the
                  `.Header` property to position its content in the template.
                properties:
                  clustered:
                    description: |-
                      Clustered, if set to true, ensures that the cluster scoped Kind of the referent will be lookup up. If the
                      referring resource is cluster scoped, then Clustered is implicitly true.
                      Defaulted to nil.
                    type: boolean
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: namespace must not be set when clustered is true
                  rule: '!(self.clustered == true) || self.namespace == ”'
              defaultMainNavigationRef:
                description: defaultMainNavigationRef is an optional reference to
                  a KDexPageNavigation resource. If not specified, no navigation will
                  be displayed. Use the `.Navigation["main"]` property to position
                  its content in the template.
                properties:
                  clustered:
                    description: |-
                      Clustered, if set to true, ensures that the cluster scoped Kind of the referent will be lookup up. If the
                      referring resource is cluster scoped, then Clustered is implicitly true.
                      Defaulted to nil.
                    type: boolean
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: namespace must not be set when clustered is true
                  rule: '!(self.clustered == true) || self.namespace == ”'
              extraNavigations:
                additionalProperties:
                  properties:
                    clustered:
                      description: |-
                        Clustered, if set to true, ensures that the cluster scoped Kind of the referent will be lookup up. If the
                        referring resource is cluster scoped, then Clustered is implicitly true.
                        Defaulted to nil.
                      type: boolean
                    name:
                      description: Name of the referent.
                      type: string
                    namespace:
                      description: |-
                        Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                        namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                        referent.
                        If the referring resource is cluster scoped, this field is ignored.
                        Defaulted to nil.
                      type: string
                  required:
                  - name
                  type: object
                  x-kubernetes-map-type: atomic
                  x-kubernetes-validations:
                  - message: namespace must not be set when clustered is true
                    rule: '!(self.clustered == true) || self.namespace == ”'
                description: extraNavigations is an optional map of named navigation
                  object references. Use `.Navigation["<name>"]` to position the named
                  navigation's content in the template.
                type: object
                x-kubernetes-validations:
                - message: '''main'' is a reserved name for an extra navigation'
                  rule: '!has(self.main)'
              scriptLibraryRef:
                description: scriptLibraryRef is an optional reference to a KDexScriptLibrary
                  resource.
                properties:
                  clustered:
                    description: |-
                      Clustered, if set to true, ensures that the cluster scoped Kind of the referent will be lookup up. If the
                      referring resource is cluster scoped, then Clustered is implicitly true.
                      Defaulted to nil.
                    type: boolean
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: |-
                      Namespace, if set, causes the lookup for the namespace scoped Kind of the referent to use the specified
                      namespace. If not set, the namespace of the resource will be used to lookup the namespace scoped Kind of the
                      referent.
                      If the referring resource is cluster scoped, this field is ignored.
                      Defaulted to nil.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: namespace must not be set when clustered is true
                  rule: '!(self.clustered == true) || self.namespace == ”'
            required:
            - content
            type: object
          status:
            description: status defines the observed state of KDexApp
            properties:
              attributes:
                additionalProperties:
                  type: string
                description: attributes hold state of the resource as key/value pairs.
                type: object
              conditions:
                description: |-
                  conditions represent the current state of the resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Progressing": the resource is being created or updated
                  - "Ready": the resource is fully functional
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed for this resource. It corresponds to the
                  resource's generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
